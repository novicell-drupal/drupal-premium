{% set classes = [
  'layout-block',
  view_mode ? 'layout-block--view-mode-' ~ view_mode|clean_class,
] %}

{% include '@molecules/divider/divider.twig' with {
  'attributes': attributes.addClass(classes)
} %}
