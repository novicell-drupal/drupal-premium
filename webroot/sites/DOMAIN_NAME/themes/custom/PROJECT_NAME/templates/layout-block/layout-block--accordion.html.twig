{% set classes = [
  'layout-block',
  view_mode ? 'layout-block--view-mode-' ~ view_mode|clean_class,
] %}

{% include '@organisms/accordion/accordion.twig' with {
  'attributes': attributes.addClass(classes),
  'headline': fields.field_accordion_headline,
  'items': items,
  'show_more_text': fields.field_accordion_show_more
} %}
