{% set classes = [
  'layout-block',
  view_mode ? 'layout-block--view-mode-' ~ view_mode|clean_class,
] %}

{% include '@molecules/quote/quote.twig' with {
  'attributes': attributes.addClass(classes),
  'quote_text': fields.field_quote_text,
  'quote_source': fields.field_quote_source
} %}
